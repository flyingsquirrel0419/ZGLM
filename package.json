{
  "name": "zglm-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Z.ai GLM Agentic CLI — GLM to the terminal",
  "scripts": {
    "build": "pnpm -r build",
    "bundle:release": "node scripts/build-release-bundle.mjs",
    "dev": "pnpm -r dev",
    "lint": "eslint 'packages/*/src/**/*.{ts,tsx}'",
    "test": "vitest run",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^20.14.0",
    "eslint": "^9.5.0",
    "tsup": "^8.1.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.58.2",
    "vitest": "^1.6.1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.4.0"
}
